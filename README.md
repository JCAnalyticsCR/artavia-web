# Cerrajería Artavia Servicio Express — Landing page

Landing one-page, solo front-end (HTML + CSS + JS sin dependencias). Mobile-first.

## Estructura
- `index.html` — página completa
- `css/styles.css` — estilos
- `js/main.js` — chat interactivo del hero
- `img/` — colocar aquí las fotos reales (ver pendientes)

## Cómo publicar
GitHub Pages: Settings → Pages → Branch `main` / carpeta raíz. No requiere build.

## Pendientes por confirmar con el cliente
- Fotos reales del local, trabajos y equipo (reemplazar los bloques `.ph`)
- Mapa de cobertura y mapa de Alajuelita (embed de Google Maps)
- Textos de reseñas de Google (solo se muestra 4.6★ / 32 opiniones)
- Tarifas y tiempo de respuesta
- Conectar Formspree: crear el formulario en formspree.io y reemplazar `REEMPLAZAR_ID` en el `action` del `<form id="form-contacto">` de `index.html`
